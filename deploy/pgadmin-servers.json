{
  "Servers": {
    "1": {
      "Name": "SimplCalCon (demo)",
      "Group": "Servers",
      "Host": "db",
      "Port": 5432,
      "MaintenanceDB": "simplcalcon",
      "Username": "postgres",
      "SSLMode": "prefer",
      "PassFile": "/tmp/pgpass",
      "Comment": "Pre-registered by docker-compose (ADR 0024). Auto-connects as the postgres superuser via the mounted pgpass (postgres/postgres, demo only)."
    }
  }
}
