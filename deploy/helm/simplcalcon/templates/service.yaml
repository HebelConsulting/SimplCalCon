apiVersion: v1
kind: Service
metadata:
  name: {{ include "simplcalcon.fullname" . }}
  labels:
    {{- include "simplcalcon.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "simplcalcon.selectorLabels" . | nindent 4 }}
